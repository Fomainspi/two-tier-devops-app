version: '3'

services:
  backend:
    build: ./backend
    container_name: backend-container-v2
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: frontend-container-v2
    ports:
      - "3000:3000"
    depends_on:
      - backend
